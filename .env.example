# Copy to `.env` and fill in from your dashboard (Publishable key).
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Google (and other OAuth) browser flows: in the Clerk dashboard, allow the redirect URL
# that matches your app scheme, e.g. recurrlyjsm://sso-callback (see app.json "scheme").
